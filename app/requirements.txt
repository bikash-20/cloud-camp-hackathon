# NutriVision backend — cut (a) skeleton
# Pinned to major+minor; patch versions are pulled by pip.

fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.6,<3.0
httpx>=0.27,<1.0

# Optional — OCR fallback for vision stub (disabled by default).
# pytesseract>=0.3.10 ; uncomment + `brew install tesseract` to enable.

# Dev
pytest>=8.0,<10.0
pytest-asyncio>=0.23,<1.0
ruff>=0.4,<1.0

"""Pydantic v2 models — source of truth on the backend side.

Per `NutriVision_AI_Blueprint.md` §8, `src/types/schemas.ts` on the
frontend and `app/models/schemas.py` here must be kept in lockstep.
Any field addition on one side requires a corresponding change on the
other.
"""
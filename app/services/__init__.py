"""Internal services — state, external clients, helpers.

These are the seams that cut (b) will replace with real implementations
(Gemini, HF, USDA). Cut (a) stubs them so the contract is exercised
without paid dependencies.
"""